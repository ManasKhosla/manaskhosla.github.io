services:
  jekyll:
    build:
      context: .
    volumes:
      - .:/app
      - bundle:/usr/local/bundle
    ports:
      - "4000:4000"
    environment:
      JEKYLL_ENV: development

volumes:
  bundle:
